#include "main.h"

/**
 * main: _isupper checks the code
 * @c: uppercase character
 *
 * Return: (1) or (0)
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
