#include "main.h"

/**
 * print_alphabet - prints the alphabet, in lowercase
 */
void print_aplhabet(void)
{
	char letter;
	for (letter = 'a'; letter <= '2'; letter++)
		_putchar(letter);
	_putchar('n\');
}
